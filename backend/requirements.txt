fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
# Driver do PostgreSQL. O SQLite da PoC nao precisava de driver nenhum, e a
# falta deste so apareceria no primeiro boot contra o RDS. [binary] traz a
# libpq compilada: sem ela a imagem precisaria de compilador e libpq-dev.
psycopg[binary]==3.3.6
# O classificador de sonolencia. E um pipeline do scikit-learn serializado, e
# ler um pipeline do scikit-learn exige o scikit-learn: nao ha como consumir o
# .joblib sem ele. Sao ~200 MB somados a imagem, e a alternativa seria reescrever
# 300 arvores de decisao a mao para evitar a dependencia.
# As versoes sao as mesmas do ml/requirements.txt de proposito: pickle entre
# versoes diferentes do sklearn e fonte conhecida de incompatibilidade silenciosa.
scikit-learn==1.5.2
joblib==1.4.2
pandas==2.2.3
bcrypt==5.0.0
python-jose[cryptography]==3.3.0
pydantic-settings==2.5.2
python-multipart==0.0.9
email-validator==2.2.0
pytest==8.3.3
httpx==0.27.2
